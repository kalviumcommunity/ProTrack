import React from "react";

function Dashboard_Child2_1({ e }) {
  return (
    <>
      

      {/* <p>
          Cluster Productivity : {(e.cluster_lines / (e.cluster_duration / 3600000)).toFixed(1)}
        </p>
        <p>
          Odd/Case Productivity : {(e.odd_case_hu / (e.odd_case_duration / 3600000)).toFixed(1)}
        </p>
        <p>
          Packing Productivity : {(e.packing_orders / (e.packing_duration / 3600000)).toFixed(1)}
        </p> */}
    </>
  );
}

export default Dashboard_Child2_1;
